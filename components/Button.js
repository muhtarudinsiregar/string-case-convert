const Button = (props) => (
    <button type="button" onClick={this.toUpperCase}>UPPERCASE</button>
    <button type="submit" onClick={this.handleSubmit}>Lowercase</button>
    <button type="submit" onClick={this.handleSubmit}>camelCase</button>
    <button type="submit" onClick={this.handleSubmit}>snake_case</button>
)

export default Button
